spring:
  application:
    name: organization-service
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8071
logging:
  level:
    com.netflix: WARN
    org.springframework.web: WARN
    com.optimagrowth: DEBUG