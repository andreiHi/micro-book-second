spring:
  application:
    name: eureka-server

  cloud:
    config:
      uri: http://localhost:8071
    loadbalancer:
      ribbon:
        enabled: false


management:
  endpoints:
    web:
      exposure:
        include: "*"
